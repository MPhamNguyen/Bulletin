# Supabase Project Configuration
# Replace these placeholders with your project details from the Supabase dashboard (Project Settings > API)
# WARNING: Only use the public 'anon' key here. Never use the 'service_role' key in client code.

SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_ANON_KEY=your-anon-public-key
